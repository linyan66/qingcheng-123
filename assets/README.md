# Demo Assets

This folder is reserved for local video-demo assets.

The current demo uses inline SVG icons in `script.js` so `index.html` can run directly without network requests or external libraries.
